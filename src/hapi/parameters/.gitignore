# The download target for `HAPI_DATA_DIR`: the Beck et al. (2016) parameter sets land here,
# and none of them belong in the repository. The downloader itself is `hapi/data/figshare.py`;
# this directory holds no code.
*
!.gitignore
